const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const logs = await prisma.exerciseLog.findMany({ where: { name: 'Barbell Bench Press' }, include: { session: true }, orderBy: { createdAt: 'desc' } }); console.log(JSON.stringify(logs, null, 2)); const memory = await prisma.workoutMemory.findFirst({ where: { exerciseName: 'Barbell Bench Press' } }); console.log(JSON.stringify(memory, null, 2)); } run().finally(() => prisma.$disconnect());