const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); const { fitnessService } = require('./src/modules/fitness/fitness.service.js'); async function run() { const cutoffDate = new Date(); cutoffDate.setDate(cutoffDate.getDate() - 28); const logs = await prisma.exerciseLog.findMany({ where: { session: { userId: 'cmqlkkv0t0000au6okbv1lu13', date: { gte: cutoffDate } }, name: 'Barbell Bench Press' }, include: { session: { select: { date: true } } }, orderBy: { createdAt: 'desc' } }); const memory = { lastWeight: 53, lastReps: 8 }; const result = fitnessService.calculateSuggestedProgression('Barbell Bench Press', memory, logs); console.log('RESULT:', JSON.stringify(result)); } run().finally(() => prisma.$disconnect());