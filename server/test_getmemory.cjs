const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); const { FitnessService } = require('./src/modules/fitness/fitness.service'); const fitnessService = new FitnessService(); async function run() { const memory = await fitnessService.getWorkoutMemory('cmqlkkv0t0000au6okbv1lu13'); console.log(JSON.stringify(memory.memories.find(m => m.exerciseName === 'Barbell Bench Press'), null, 2)); } run().finally(() => prisma.$disconnect());