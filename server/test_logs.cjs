const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const cutoffDate = new Date(); cutoffDate.setDate(cutoffDate.getDate() - 28); const logs = await prisma.exerciseLog.findMany({ where: { session: { userId: 'cmqlkkv0t0000au6okbv1lu13', date: { gte: cutoffDate } }, name: 'Barbell Bench Press' }, include: { session: { select: { date: true } } }, orderBy: { createdAt: 'desc' } }); console.log('LOGS:', JSON.stringify(logs, null, 2)); } run().finally(() => prisma.$disconnect());